version: 1

starts:
  - id: index
    label: "INDEX"
    path: "/index/index.x.yml"

  - id: error
    label: "ERROR"
    path: "/error/error.x.yml"

  - id: start8
    label: "START8"
    path: "/start8/start8.x.yml"

  - id: read
    label: "READ"
    path: "/read/read.x.yml"

options:
  show_all: true
  auto_discover: true
  sort: alphabetical
